---
# Book settings
# Learn more at https://jupyterbook.org/customize/config.html

title: NCPA Policies
author: NEM
logo: ncpa-logo.jpg
copyright: '2025'

# Force re-execution of notebooks on each build.
# See https://jupyterbook.org/content/execute.html
execute:
  execute_notebooks: force

# Add a "Launch on Google Colab" button to .ipynb files
launch_buttons:
  colab_url: "https://colab.research.google.com"

# Define the name of the latex output file for PDF builds
latex:
  latex_documents:
    targetname: ncpaPolicyDocs.tex

# Add a bibtex file so that we can create citations
bibtex_bibfiles:
 - references.bib

# Information about where the book exists on the web
repository:
  url: https://github.com/docNathanM/ncpa-policies  # Online location of your book
  path_to_book: book  # Optional path to your book, relative to the repository root
  branch: main  # Which branch of the repository should be used when creating links (optional)

# Add GitHub buttons to your book
# See https://jupyterbook.org/customize/config.html#add-a-link-to-your-repository
html:
  favicon: favicon.ico
  use_issues_button: true
  use_repository_button: true
  extra_css: ["_static/custom.css"] # Add some custom CSS tweaking.

parse:
#   # myst_substitutions:
#   #   sub3: My _global_ value!
#   myst_enable_extensions: # default extensions to enable in the myst parser. See https://myst-parser.readthedocs.io/en/latest/using/syntax-optional.html
#     - amsmath
#     - colon_fence
#     - deflist
#     - dollarmath
#     - html_admonition
#     - html_image
#     - linkify
#     - replacements
#     - smartquotes
#     - substitution
    myst_url_schemes: [mailto, http, https] # URI schemes that will be recognised as external URLs in Markdown links
    myst_links_external_new_tab: true
    
# Uncomment to use mermaid diagrams
# sphinx:
  # extra_extensions:
    # - sphinxcontrib.mermaid
